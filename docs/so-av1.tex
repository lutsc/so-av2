\documentclass[12 pt]{abntex2}
\usepackage[utf8]{inputenc} % Codificação de caracteres
% \usepackage[T1]{fontenc} % Seleção de fonte
\usepackage[brazil]{babel} % Idioma do documento
\usepackage{graphicx} %For figures
\usepackage{float} %For figures
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{rsfso}
\usepackage{mathrsfs}
\usepackage{lmodern}
\usepackage{hyperref}
\usepackage{listings}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
    pdftitle={},
    pdfpagemode=FullScreen,
}


\hbadness=10000 % Suppresses Underfull \hbox warnings
\hfuzz=9999pt  % Suppresses Overfull \hbox warnings

% \usepackage{pgfplots}
% \pgfplotsset{compat=1.18}

% \begin{tikzpicture}
% \begin{axis}[
%     axis lines = middle, % Draws axes through the origin
%     xlabel = $x$,
%     ylabel = {$f(x)$},
%     xmin=-5, xmax=5,
%     ymin=-5, ymax=5,
%     samples=100, % Increase samples for smoother curves
% ]
% \addplot[domain=-5:5] {x^2}; % Plots the function y = x^2
% \end{axis}
% \end{tikzpicture}


\makeatletter
\renewcommand{\thesection}{%
  \ifnum\c@chapter<1 \@arabic\c@section
  \else \thechapter.\@arabic\c@section
  \fi
}
\makeatother

    % \begin{figure}[H]
    %   \centering
    %   \includegraphics[width =0.50\linewidth, angle = 0]{file.png}
    %   \caption{caption about}
    %   \label{} %if necessary
    % \end{figure}
    % Code to add figures

    % \begin{table}[h!]
    %     \centering
    %     \caption{Example Table}
    %     \label{tab:my_table}
    %     \begin{tabular}{|l|c|r|}
    %         \hline
    %         \textbf{Column 1} & \textbf{Column 2} & \textbf{Column 3} \\
    %         \hline
    %         Row 1, Cell 1 & Row 1, Cell 2 & Row 1, Cell 3 \\
    %         Row 2, Cell 1 & Row 2, Cell 2 & Row 2, Cell 3 \\
    %         \hline
    %     \end{tabular}
    % \end{table}
    % Code for adding tables

\autor{Lucas Tasca, Melkior Gabriel Paim Misiak}
\titulo{Avaliação M1 \\ IPC, Threads e Paralelismo}
\orientador{Felipe Viel}
\instituicao{Universidade do Vale do Itajaí - UNIVALI\\ Sistemas Operacionais}
\local{Itajaí - Santa Catarina}
\data{\today}

\begin{document}

\imprimirfolhaderosto

\tableofcontents*
\clearpage

\section{Introdução}

Este projeto apresenta a implementação de um sistema de processamento de imagens do tipo PGM P5 (Portable Gray Map em binário), 
utilizando a linguagem de programação C, com o objetivo de consolidar o aprendizado de IPC, concorrência, threads e paralelismo.

O sistema é dividido dois processos: $sender$ e $worker$, onde o $sender$ é responsável por ler uma imagem do tipo PGM P5, 
decompõe seus metadados e pixels e os envia a uma FIFO, enquanto o $worker$ é responsável por ler o que foi enviado à FIFO, 
processando os dados por um filtro escolhido e dividindo as tarefas de processamento entre uma quantidade de threads, 
ambos dados por argumentos, e por fim escrevendo a imagem resultante.

\section{Implementação}

\subsection{Processo Emissor (Sender)}

Primeiramente, o processo emissor cria a FIFO, caso ela não exista.

\begin{lstlisting}[language=C]
    mkfifo(args.fifo, 0666);
\end{lstlisting}

O emissor então lê a imagem PGM P5, decompõe seus metadados e pixels, 
e os separando nas variáveis PGM e Header, respectivamente.

\begin{lstlisting}[language=C]
    PGM pgm;
    read_pgm(args.input_file, &pgm);

    Header header;
    header.w = pgm.w;
    header.h = pgm.h;
    header.maxv = pgm.maxv;
\end{lstlisting}

O emissor abre a FIFO para escrita em binário, o que bloqueia o processo emissor até que o 
processo trabalhador a abra para leitura.

\begin{lstlisting}[language=C]
    FILE* fd = fopen(args.fifo, "wb");
\end{lstlisting}

O emissor então escreve os metadados e os pixels da imagem na FIFO, para que o 
processo trabalhador possa ler.

\begin{lstlisting}[language=C]
  fwrite(&header, sizeof(Header), 1, fd);
  fwrite(pgm.data, 1, img_size, fd);
\end{lstlisting}

Enfim a FIFO é fechada e os recursos alocados para a imagem são liberados.

\begin{lstlisting}[language=C]
    free(pgm.data);
    fflush(fd);
    fclose(fd);
\end{lstlisting}  

\subsection{Processo Trabalhador (Worker)}


\end{document}
